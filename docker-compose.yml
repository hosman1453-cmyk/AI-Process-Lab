services:
  n8n:
    image: n8nio/n8n:latest
    container_name: ai-process-n8n
    restart: always

    ports:
      - "5678:5678"

    volumes:
      - n8n_data:/home/node/.n8n

    environment:
      - TZ=Europe/Berlin

volumes:
  n8n_data: